---
import { friendLinks, shuffleLinks } from '../../data/friend-links'
import LinkCard from './LinkCard.astro'

const links = shuffleLinks(friendLinks)
---

<div class="grid grid-cols-1 sm:grid-cols-2 gap-3 sm:gap-4 my-2">
  {links.map(link => (
    <LinkCard {...link} />
  ))}
</div>
